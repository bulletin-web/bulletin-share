# ===================================
#  Field Definitions
# ===================================

fields:

    name:
        label: backend::lang.user.role.name_field
        span: left

    description:
        label: backend::lang.user.role.description_field
        type: textarea
        size: small
        span: left

tabs:
    stretch: true
